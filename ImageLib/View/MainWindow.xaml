<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:idc="clr-namespace:Dock.Avalonia.Controls;assembly=Dock.Avalonia"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        xmlns:local="clr-namespace:ImageLib.Controls;assembly=ImageLib"
        x:Class="ImageLib.Controls.MainWindow"
        Title="{Binding Title}">

    <!--<Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>-->

    <!--<idc:DockControl Layout="{Binding Layout}"/>-->
    <DockPanel>
        <Menu MinHeight="24" DockPanel.Dock="Top" Items="{Binding Menu}" Name="Menu">
           <Menu.Styles>
                <Style Selector="MenuItem">
                    <Setter Property="Header" Value="{Binding Header}" />
                    <Setter Property="Items" Value="{Binding Items}" />
                    <Setter Property="Command" Value="{Binding Command}"/>
                    <Setter Property="CommandParameter" Value="{Binding CommandParameter}"/>
                </Style>
            </Menu.Styles>
        </Menu>
        <idc:DockControl Layout="{Binding Layout}"/>
        <!--<local:DockView Layout="{Binding Layout}"/>-->
    </DockPanel>
</Window>
